"""Domain claims — factual, temporal, and grounding primitives.

Transferred from the graph-engineering research project (R-008 architecture).
Deterministic, stdlib-only, LLM-free core: unitizer (D-1), temporal resolver,
grounding evaluator, entity resolution, traversal/cite helpers, DAG planner,
and reflective loop.
"""
