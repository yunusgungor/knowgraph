## Experiment: E-002 — Traversal return-type annotation contract
- **Date:** 09.09.2026
- **Status:** completed
- **Theory:** E-001 review found an annotation lie: traverse_graph_reference_aware is annotated `-> set[UUID]` but returns `sorted(visited)` (a `list`). Two call sites in retriever.py slice the result (`expanded_node_ids[:node_cap]`), which raises TypeError on a real set — so callers already depend on list behavior and the annotation (not the code) is wrong.
- **Hypothesis:** H-002: "type_accuracy >= 0.90"
- **Measurement Metrics:** type_accuracy >= 0.90 (fraction of 40 randomized-graph trials where reference_aware returns a sorted list AND dfs/bfs/reverse_references return sets)
- **Experiment Design:** Bench builds 40 seeded random graphs (same generator shape as E-001: 12 nodes, 10-25 edges, reference/call/data_flow/hierarchy/control_flow + semantic + temporal edges), runs all four traversal functions per trial with 1-3 hops from 2 seeds, and scores 1 iff isinstance(ref_result, list) and ref_result == sorted(ref_result) and the other three return sets. Controlled variables: fixed seeds (2000+t, reproducible), stdlib only, no LLM/Joern. Repeatable via: python scripts/bench/bench_traversal_annotation.py. Falsifiability: a broken implementation returning the raw unsorted set for reference_aware scores 0.00 (isinstance fails every trial); dropping sorted() scores 0.00 (order check fails).
- **Sample Size n:** 40 trials (95% Wilson lower bound of 40/40 clears 0.90)
- **Code Scope:** tests/** knowgraph/**
- **Raw Results:** measured=1.0; n=40
- **Uncertainty:** none (n=40, 95% Wilson lower bound 0.91 >= threshold 0.9)
- **Metric:** consistent (measured type == claimed type)
- **Decision:** APPROVED — H-002: measured=1.0 >= threshold=0.9
- **Gate Evidence:** measured=1.0 claim="type_accuracy >= 0.90" GATE-OK-E-002-73f23537fa6ded8ec2ae78bf2febc6ed
- **Next Step:** Proceed to Code
- **Measurement Command:** python scripts/bench/bench_traversal_annotation.py
