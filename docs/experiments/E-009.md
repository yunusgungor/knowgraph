## Experiment: E-009 — Extend regression lock with retriever order bench
- **Date:** 09.09.2026
- **Status:** completed
- **Theory:** E-006 locked the traversal guarantees (189 checks) but predates E-007/E-008 — the retriever order guarantee (dedupe+sort tail, honest both-loaders-mocked bench) is not in the lock. A future change to enrich_with_conversations or retriever.py can silently re-break tail order and the one-command lock stays green. Extending the lock with the order bench (40 trials) brings it to 229 checks; threshold >= 0.95 still fails on any single-guarantee break.
- **Hypothesis:** H-009: "regression_accuracy >= 0.95"
- **Measurement Metrics:** regression_accuracy >= 0.95 (fraction of 229 checks passing: 40 determinism + 40 annotation + 100 reachability + 40 retriever-order trials + 9 pytest tests)
- **Experiment Design:** Extend scripts/bench/bench_traversal_regression.py with the order bench as a fourth subprocess suite, then run it as the measurement command. Controlled variables: identical seeds/generators to E-001/E-002/E-005/E-008, stdlib + pytest-via-subprocess only. Repeatable via: python scripts/bench/bench_traversal_regression.py. Falsifiability: reverting the E-008 fix fails ~19 order checks (~0.92 < 0.95); reverting sorted() fails ~80 (~0.65); breaking the annotation fails 40 (~0.83) — every single-guarantee break scores below 0.95.
- **Sample Size n:** 229 checks (95% Wilson lower bound of 229/229 ≈ 0.98 clears 0.95)
- **Code Scope:** tests/** knowgraph/** scripts/bench/**
- **Raw Results:** measured=1.0; n=229
- **Uncertainty:** none (n=229, 95% Wilson lower bound 0.98 >= threshold 0.95)
- **Metric:** consistent (measured regression == claimed regression)
- **Decision:** APPROVED — H-009: measured=1.0 >= threshold=0.95
- **Gate Evidence:** measured=1.0 claim="regression_accuracy >= 0.95" GATE-OK-E-009-fffd94827289ea316c77095cec445f17
- **Next Step:** Proceed to Code
- **Measurement Command:** python scripts/bench/bench_traversal_regression.py
