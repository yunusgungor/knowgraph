## Experiment: E-005 — Shortest-path re-expansion fix for reference-aware traversal
- **Date:** 09.09.2026
- **Status:** completed
- **Theory:** E-004 APPROVED at 0.95 but post-gate analysis showed the fix was incomplete: the `not in visited` re-enqueue guard still inherits inflated depth — a node visited via a long path never re-expands at its shorter depth, so its neighbors keep the inflated depth and get cut (trials 17/37). Revised theory: a neighbor must re-enqueue whenever a strictly shorter path is discovered (`not in best or depth+1 < best[neighbor]`), not merely when unvisited. Combined with E-004's stale-skip, every node then both lands and expands along a shortest path.
- **Hypothesis:** H-005: "reachability_accuracy >= 0.95"
- **Measurement Metrics:** reachability_accuracy >= 0.95 (same oracle and trial shape as E-003/E-004: fraction of 40 seeded-graph trials where the returned set exactly equals the true-BFS oracle; stronger 0.95 bar since the mechanism is now fully shortest-path)
- **Experiment Design:** The fix is applied to knowgraph/domain/algorithms/traversal.py, then the unchanged reachability bench (scripts/bench/bench_traversal_reachability.py, seeds 3000+t) is the measurement command. Scores 1 per trial iff the returned set exactly equals the oracle. Controlled variables: identical graph generator/seeds to E-003/E-004, stdlib only, no LLM/Joern. Falsifiability: the E-004 code scores 0.95 on this exact bench; the `not in visited` variant stays below 1.00 (trials 17/37 fail); an exact shortest-path implementation scores 1.00.
- **Sample Size n:** 100 trials (95% Wilson lower bound of 100/100 clears 0.95)
- **Code Scope:** tests/** knowgraph/** scripts/bench/**
- **Raw Results:** measured=1.0; n=100
- **Uncertainty:** none (n=100, 95% Wilson lower bound 0.96 >= threshold 0.95)
- **Metric:** consistent (measured reachability == claimed reachability)
- **Decision:** APPROVED — H-005: measured=1.0 >= threshold=0.95
- **Gate Evidence:** measured=1.0 claim="reachability_accuracy >= 0.95" GATE-OK-E-005-67571ec05a15001ce88b5c3d1a7ebef7
- **Next Step:** Proceed to Code
- **Measurement Command:** python scripts/bench/bench_traversal_reachability100.py
