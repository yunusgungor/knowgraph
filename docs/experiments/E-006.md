## Experiment: E-006 — Traversal regression lock (E-001..E-005 in one command)
- **Date:** 09.09.2026
- **Status:** completed
- **Theory:** E-001..E-005 established three guarantees for reference-aware traversal (sorted stable return; list-not-set annotation contract; true-BFS reachability), each measured by its own bench. Without a lock, a future change can silently break any of them and no single command catches it. A regression bench that re-runs all three trial suites plus the traversal/retriever pytest files in one measurement locks the guarantees; threshold >= 0.95 with n=189 gives a clean Wilson pass on green and fails on any single-guarantee break.
- **Hypothesis:** H-006: "regression_accuracy >= 0.95"
- **Measurement Metrics:** regression_accuracy >= 0.95 (fraction of 189 checks passing: 40 determinism trials + 40 annotation trials + 100 reachability trials + 9 pytest tests, all counted as individual pass/fail checks)
- **Experiment Design:** scripts/bench/bench_traversal_regression.py runs the three existing bench scripts as subprocesses (parsing their (w/t) lines) plus pytest over tests/test_traversal.py, tests/test_traversal_comprehensive.py, tests/test_retriever.py, and prints regression_accuracy=W/T (W/T). Controlled variables: identical seeds/generators to E-001/E-002/E-005, stdlib only (pytest via subprocess), no LLM/Joern. Repeatable via: python scripts/bench/bench_traversal_regression.py. Falsifiability: reverting sorted() fails ~80 checks (~0.58); reverting the E-005 fix fails ~18 reachability checks (~0.90); breaking the annotation fails 40 checks (~0.79) — every single-guarantee break scores below 0.95.
- **Sample Size n:** 189 checks (95% Wilson lower bound of 189/189 ≈ 0.98 clears 0.95)
- **Code Scope:** tests/** knowgraph/** scripts/bench/**
- **Raw Results:** measured=1.0; n=189
- **Uncertainty:** none (n=189, 95% Wilson lower bound 0.98 >= threshold 0.95)
- **Metric:** consistent (measured regression == claimed regression)
- **Decision:** APPROVED — H-006: measured=1.0 >= threshold=0.95
- **Gate Evidence:** measured=1.0 claim="regression_accuracy >= 0.95" GATE-OK-E-006-5faf6ef959d534d760cce63cc2d57f76
- **Next Step:** Proceed to Code
- **Measurement Command:** python scripts/bench/bench_traversal_regression.py
