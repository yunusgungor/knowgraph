## Experiment: E-003 — Traversal reachability completeness
- **Date:** 09.09.2026
- **Status:** REJECTED
- **Theory:** E-001 diagnosis found that the heap priority ordering in traverse_graph_reference_aware inflates first-pop depth: a node reachable in 1 hop can first pop via a depth-3 priority path and get cut by max_hops (or skipped). A probe over 200 seeded graphs (two seed bases) showed ~18% of trials missing nodes that a true BFS reaches. If reachability is incomplete, retrieval silently drops code dependencies.
- **Hypothesis:** H-003: "reachability_accuracy >= 0.90"
- **Measurement Metrics:** reachability_accuracy >= 0.90 (fraction of 40 randomized-graph trials where set(traverse_graph_reference_aware(...)) exactly equals the reachable set of an independent true-BFS oracle with the same edge semantics: reference/call/data_flow/hierarchy/control_flow directed, semantic undirected, supersedes/contradicts excluded)
- **Experiment Design:** Bench builds 40 seeded random graphs (12 nodes, 10-25 edges, same generator shape as E-001/E-002), runs traverse_graph_reference_aware with 1-3 hops from 2 seeds per trial, and scores 1 iff the returned set exactly equals the oracle's set. Controlled variables: fixed seeds (3000+t, reproducible), stdlib only, no LLM/Joern. Repeatable via: python scripts/bench/bench_traversal_reachability.py. Falsifiability: the oracle disagrees with a depth-inflating implementation on ~18% of trials (probe: 17-19/100), so a broken implementation scores ~0.82 < 0.90; an exact BFS scores 1.00.
- **Sample Size n:** 40 trials (95% Wilson lower bound of 40/40 clears 0.90)
- **Code Scope:** tests/** knowgraph/**
- **Raw Results:** measured=0.8; n=40
- **Uncertainty:** n=40 (small sample: 95% Wilson lower bound 0.65 < threshold 0.9)
- **Metric:** consistent (measured reachability == claimed reachability)
- **Decision:** REJECTED — H-003: measured=0.8 >= threshold=0.9 (gate FAIL)
- **Gate Evidence:** <gate writes: GATE-OK-...>
- **Next Step:** Return to Theory; open new experiment for new hypothesis
- **Measurement Command:** python scripts/bench/bench_traversal_reachability.py
