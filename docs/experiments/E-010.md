## Experiment: E-010 — Diagnostic slow-provider hint references removed knobs
- **Date:** 09.09.2026
- **Status:** completed
- **Theory:** v1.1.1 removed the timeout env overrides (KNOWGRAPH_LLM_REQUEST_TIMEOUT / SYNTHESIS / QUERY_TOTAL) and fixed the timeouts as defaults (60/120/120s); the CHANGELOG claims the diagnostic hints no longer reference the env vars. But the slow-provider recommendation in diagnostic_handler.py still names KNOWGRAPH_LLM_REQUEST_TIMEOUT (test_recommendation_for_60s_timeout fails: hint text lacks the var) and tells the user to raise the MCP client timeout — advice that is unactionable since the knob no longer exists. A diagnostic that recommends a removed knob is worse than silence: it sends the user editing env vars that do nothing.
- **Hypothesis:** H-010: "hint_accuracy >= 0.90"
- **Measurement Metrics:** hint_accuracy >= 0.90 (fraction of 40 diagnostic-report scenarios where the report's recommendation section is correct: fires the slow-provider hint with actionable advice when a provider is configured, stays silent when no provider exists, and never names a removed env var)
- **Experiment Design:** Bench calls handle_diagnostic (asyncio.run, real handler, no disk/LLM) over 40 scenarios: provider configured/unconfigured × timeout 30/59/60/61/120 × graph missing/present × top_k low/normal (full factorial plus timeout-boundary repeats at the <=60 firing edge). Scenario scores 1 iff: (a) no configured provider → no timeout hint; (b) provider + timeout<=60 → hint present AND names no removed var AND gives actionable advice (faster endpoint / reduce scope); (c) provider + timeout>60 → no timeout hint; (d) top_k<15 → depth hint present regardless. Controlled variables: fixed scenario matrix, stdlib + asyncio only. Repeatable via: python scripts/bench/bench_diagnostic_hint.py. Falsifiability: the stale test (expecting the removed var + an MCP-client sentence the handler never had in this wording) fails on current code → the suite is red; a handler/test pair that agrees on removed-var-free actionable advice scores 1.00 here AND turns the suite green.
- **Sample Size n:** 40 scenarios — full 2×3×2×2 factorial (24) plus 16 timeout-boundary repeats at 59/60/61 (95% Wilson lower bound of 40/40 ≈ 0.91 clears 0.90)
- **Code Scope:** tests/** knowgraph/** scripts/bench/**
- **Raw Results:** measured=1.0; n=40
- **Uncertainty:** none (n=40, 95% Wilson lower bound 0.91 >= threshold 0.9)
- **Metric:** consistent (measured hint == claimed hint)
- **Decision:** APPROVED — H-010: measured=1.0 >= threshold=0.9
- **Gate Evidence:** measured=1.0 claim="hint_accuracy >= 0.90" GATE-OK-E-010-d77e38d15245fb6899f7be9ccae0f98b
- **Next Step:** Proceed to Code
- **Measurement Command:** python scripts/bench/bench_diagnostic_hint.py
- **Note (post-gate correction, integrity rule 6):** the bench measured the UNMODIFIED handler at 1.00 — the Theory sentence "the recommendation still names KNOWGRAPH_LLM_REQUEST_TIMEOUT" was proven FALSE by the probe (has-removed-var: False). The handler is already clean; what is actually stale is (a) the test asserting the removed var + an MCP-client sentence, and (b) docs/USER_GUIDE.md documenting the removed vars in 4 places. Code phase fixes the stale test + stale docs, not the handler.
