## Experiment: E-011 — Fast test-suite baseline lock (924 green)
- **Date:** 09.09.2026
- **Status:** completed
- **Theory:** E-010's fix turned the fast suite green (924 passed, 28 skipped, 1 xfailed, 64 slow/integration deselected — Joern/MCP/e2e need binaries and servers). Without a lock, a future change can silently break the suite and no single bench catches it: the traversal lock (E-009) covers only 9 test files. A baseline bench that runs the full fast suite as one measurement and scores pass-rate locks the green state; n=924 gives a Wilson bound that tolerates zero failures at a >= 0.99 bar.
- **Hypothesis:** H-011: "suite_accuracy >= 0.99"
- **Measurement Metrics:** suite_accuracy >= 0.99 (fraction of collected fast-suite tests passing: passed / (passed + failed); skips and deselects excluded, xfail counted as pass — matches pytest's own accounting)
- **Experiment Design:** scripts/bench/bench_fast_suite.py runs pytest over tests/ with the same deselection as the green run (--ignore integration/fixtures, -k "not joern and not e2e and not mcp and not slow") as a subprocess, parses the "N passed, M skipped" line, and prints suite_accuracy=P/(P+F) (P+F). Controlled variables: identical selection flags, no code under test touched, no LLM/servers. Repeatable via: python scripts/bench/bench_fast_suite.py. Falsifiability: the pre-E-010 suite scored 923/924 ≈ 0.999 — above bar, so this bench alone would NOT have caught that single failure; its value is locking the green state against future multi-failure regressions (2+ failures fail the bar). A suite with 10 failures scores ~0.989 < 0.99.
- **Sample Size n:** ~924 tests (95% Wilson lower bound of 924/924 ≈ 0.996 clears 0.99)
- **Code Scope:** tests/** knowgraph/** scripts/bench/**
- **Raw Results:** measured=1.0; n=924
- **Uncertainty:** none (n=924, 95% Wilson lower bound 1.00 >= threshold 0.99)
- **Metric:** consistent (measured suite == claimed suite)
- **Decision:** APPROVED — H-011: measured=1.0 >= threshold=0.99
- **Gate Evidence:** measured=1.0 claim="suite_accuracy >= 0.99" GATE-OK-E-011-ffc0cfe7fef65b2048ed7cb9b6ce5eac
- **Next Step:** Proceed to Code
- **Measurement Command:** python scripts/bench/bench_fast_suite.py
