## Experiment: E-004 — Fix priority-inflated depth cutoff in reference-aware traversal
- **Date:** 09.09.2026
- **Status:** completed
- **Theory:** E-003 REJECTED (0.80 < 0.90): the heap priority ordering in traverse_graph_reference_aware inflates first-pop depth — a 1-hop node first popped via a depth-3 priority path gets cut by max_hops. Revised theory: reachability must be decided by shortest-path (BFS) depth while keeping the priority heap for expansion allocation. Fix: track per-node best-known depth, skip stale heap entries, re-enqueue on shorter-path discovery, and cut expansion (not membership) at max_hops — every first-visit then happens along a shortest path, so the reachable set equals true BFS.
- **Hypothesis:** H-004: "reachability_accuracy >= 0.90"
- **Measurement Metrics:** reachability_accuracy >= 0.90 (same oracle and trial shape as E-003: fraction of 40 seeded-graph trials where the returned set exactly equals the true-BFS oracle)
- **Experiment Design:** Apply the fix to knowgraph/domain/algorithms/traversal.py, then run the unchanged E-003 bench (scripts/bench/bench_traversal_reachability.py, seeds 3000+t) as the measurement command. Scores 1 per trial iff the returned set exactly equals the oracle. Controlled variables: identical graph generator/seeds to E-003, stdlib only, no LLM/Joern. Falsifiability: the unfixed code scored 0.80 on this exact bench; only the fix can move it above 0.90.
- **Sample Size n:** 40 trials (95% Wilson lower bound of 40/40 clears 0.90)
- **Code Scope:** tests/** knowgraph/** scripts/bench/**
- **Raw Results:** measured=0.95; n=40
- **Uncertainty:** n=40 (small sample: 95% Wilson lower bound 0.83 < threshold 0.9)
- **Metric:** consistent (measured reachability == claimed reachability)
- **Decision:** APPROVED — H-004: measured=0.95 >= threshold=0.9
- **Gate Evidence:** measured=0.95 claim="reachability_accuracy >= 0.90" GATE-OK-E-004-836db2ba362b3367bf39a36f4b1e21bb
- **Next Step:** Proceed to Code
- **Measurement Command:** python scripts/bench/bench_traversal_reachability.py
